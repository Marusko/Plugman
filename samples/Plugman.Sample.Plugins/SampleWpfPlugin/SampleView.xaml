<UserControl x:Class="SampleWpfPlugin.SampleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SampleWpfPlugin"
             d:DataContext="{d:DesignInstance Type=local:SampleViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" />

        <TextBlock Grid.Row="1"
                   Margin="0,4,0,12"
                   TextWrapping="Wrap"
                   Foreground="#555"
                   Text="This whole panel — view, view model and bindings — was built inside the plugin. The host only hosted the FrameworkElement that CreateView returned." />

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Tick" Padding="12,4" Command="{Binding TickCommand}" />
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Text="{Binding Status}" />
        </StackPanel>

        <Border Grid.Row="3" Margin="0,12,0,0" Background="#F4F4F4" CornerRadius="4" Padding="12">
            <TextBlock TextWrapping="Wrap" Text="{Binding DataDirectory}" FontFamily="Consolas" />
        </Border>
    </Grid>
</UserControl>
